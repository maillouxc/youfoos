// This variable holds production mode environment variables
// This const will appear unused in an IDE, but it will be dynamically replaced at runtime
export const environment = {
  production: true,
  apiUrl: 'http://youfoos:80/api'
};
